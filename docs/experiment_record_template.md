# AI_Bell 实验尝试登记模板

> 用途：统一记录“这次尝试是什么、为什么做、做到哪一步、下一步是什么”，为后续重构提供清晰依据。  
> 使用方式：每个尝试复制一份本模板并填写；建议文件名：`YYYY-MM-DD_<场景>_<方法>_<关键词>.md`

---

## 0. 基本信息

- `尝试ID`:
- `标题`:
- `日期`:
- `负责人`:
- `状态`: `planned` / `running` / `blocked` / `completed` / `archived`
- `优先级`: `P0` / `P1` / `P2`

## 1. 尝试分类（核心标识）

- `问题场景`: `222` / `322` / `n22` / `其他`
- `方法类型`: `MLP拟合` / `DQN策略学习` / `LRS枚举` / `解析/几何方法` / `其他`
- `目标类型`: `量子边界拟合` / `边界搜索策略` / `全边界枚举` / `验证与基准`

## 2. 目标与假设

- `研究问题`:
- `核心假设`:
- `成功标准（可量化）`:
  - 指标1:
  - 指标2:
  - 指标3:

## 3. 代码与数据定位

- `主入口脚本`:
- `关键模块文件`:
  - 
  - 
- `依赖工具/求解器`: 例如 `MOSEK` / `lrs` / `PyTorch`
- `数据来源`:
- `输出产物`:
  - 模型/权重:
  - 日志:
  - 图表:
  - 中间结果:

## 4. 实验配置

- `硬件环境`: CPU/GPU/内存
- `软件环境`: Python/关键库版本
- `随机种子`:
- `关键超参数`:
  - 
  - 
- `训练/求解预算`: 如 episode 数、时长、迭代次数

## 5. 实验流程（可复现）

1. `准备`:
2. `运行命令`:
3. `结果收集`:
4. `结果分析`:

## 6. 结果记录

- `主要结果（定量）`:
- `对比基线`:
- `典型可视化/样例`:
- `是否达到成功标准`: `是` / `否` / `部分达到`

## 7. 问题与风险

- `当前问题`:
- `失败模式`:
- `潜在原因`:
- `风险等级`: `高` / `中` / `低`

## 8. 结论与下一步

- `结论（一句话）`:
- `建议保留内容`:
- `建议归档内容`:
- `下一步动作（最多3条）`:
  1. 
  2. 
  3. 

## 9. 重构映射（给 step2 用）

- `建议归属目录`: 例如 `experiments/222/dqn/`、`experiments/322/lrs/`
- `是否需要保留兼容入口`: `是` / `否`
- `重命名建议`:
  - 旧路径 -> 新路径
  - 旧路径 -> 新路径
- `测试需求`:
  - 
  - 

---

## 附：当前项目可先登记的尝试清单（待你补全）

1. `MLP 拟合 222 量子边界`
2. `DQN 学习 222 场景边界搜索策略`
3. `DQN 学习 322 场景边界搜索策略`
4. `使用 lrs 枚举 322 全部边界`

