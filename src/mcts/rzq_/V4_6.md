# v4.6 prior/random交替的六bucket消融

入口 `run_class8_v4_6.py` 继承v4.5，保留6个bucket及相同的渐进扩展容量。

- bucket 0、2、4：compatibility-richness prior。
- bucket 1、3、5：在尚未展开的动作中均匀随机。
- selection继续使用原始父边Q、真实节点visits，最低访问保障为1。

默认输出 `runs/class8_v4_6_300_trace/`。
