# v4.5 全均匀随机 expansion 消融

入口 `run_class8_v4_5.py` 继承 v4.4，只取消 bucket 0 的 compatibility-richness prior。

- bucket 0–5：全部在尚未展开的动作中均匀随机抽样。
- bucket 数仍为6，因此保持 v4.4 的渐进扩展容量不变。
- selection 继续使用原始父边Q、真实节点visits，最低访问保障为1。

```bash
src/mcts/rzq_/.venv/bin/python src/mcts/rzq_/run_class8_v4_5.py
```

默认输出 `runs/class8_v4_5_300_trace/`。
