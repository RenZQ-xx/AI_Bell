[project]
name = "rzq-mcts-experiments"
version = "0.1.0"
description = "Isolated environment for current MCTS decision-trace experiments"
requires-python = "==3.12.3"
dependencies = ["numpy==2.4.0"]

[tool.uv]
package = false
